# Document conversion dependencies (add to pyproject.toml or install separately)
# Core conversion (always needed)
markitdown[all]  # Microsoft's document to Markdown converter

# Advanced PDF conversion (optional but recommended)
# Note: Requires Python 3.12, not compatible with Python 3.13+
mineru[all]  # Advanced PDF conversion with OCR and formula support
magic-pdf    # Core MinerU library (installed with mineru)

# Installation commands:
# uv pip install markitdown[all]
# uv pip install "mineru[all]"  # Python 3.12 only

# Environment variables for MinerU:
# export MINERU_MODEL_SOURCE=modelscope  # Use modelscope mirror (faster in Asia)
# export MINERU_MODEL_SOURCE=huggingface  # Default source
